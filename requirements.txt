discord.py>=2.4.0
python-dotenv==1.0.0
aiohttp>=3.10.0
Flask==3.1.3
Werkzeug>=3.1.6
aiosqlite==0.19.0
posthog>=3.6.0
bandit>=1.7.5
pip-audit>=2.6.0
anthropic>=0.40.0
# optional (voice STT, lazy-imported — NOT required to run the bot):
#   deepgram-sdk    # cloud STT (Nova-3); needs DEEPGRAM_API_KEY
#   faster-whisper  # local STT model (pulls in numpy / ctranslate2)
# optional (live Discord voice receive, lazy-imported): discord-ext-voice-recv
# optional (TTS speak-back / personas, lazy-imported — NOT required to run the bot):
#   elevenlabs      # cloud TTS (Flash v2.5); needs ELEVENLABS_API_KEY
